# my-project

A basic Express.js server.

## Setup

```bash
npm install
```

## Usage

```bash
node index.js
```

The server runs on http://localhost:3000.

## API

- `GET /` — Returns a JSON greeting
